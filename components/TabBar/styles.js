import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 40,
    flexDirection: 'row',
    borderTopWidth: 1,
    borderTopColor: '#dddddd',
  },
});

export { styles };
